To use the script above, put it in your source code and call 
`extendArray()` without argument to extend any array automatically 
from now on or `extendArray(myArray)` to apply the array extension to a single
object.
